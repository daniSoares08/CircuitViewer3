#include "cv3.h"

/*
 * V3 starts without formula content from V2.
 * Add FormulaTopic entries here when a new batch needs formulas.
 */

const FormulaTopic formula_topics[] = {
    { "Sem formulas", 0, 0 }
};

const uint8_t formula_count = 0;
